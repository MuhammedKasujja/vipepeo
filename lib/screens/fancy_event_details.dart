import 'package:flutter/material.dart';
import 'package:vipepeo_app/models/models.dart';

class FancyEventPage extends StatelessWidget {
  final Event event;

  const FancyEventPage({Key key, @required this.event}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
